#src
fun sum(to: int): int {
    var sum = 0;
    var i = 1;
    while (i <= to) {
        sum = sum + i;
        i = i + 1;
    }
    return sum;
}

#expect
fun sum(to.0: int): int
    copy sum.1 = int 0
    copy i.2 = int 1
    .start_1:
    jmp .end_2 if int i.2 > to.0
    sum.1 = int sum.1 + i.2
    i.2 = int i.2 + 1
    jmp .start_1
    .end_2:
    copy retval.3 = int sum.1
    jmp .ret_0
    .ret_0:

